function SetUsername(username){
    // comlex DB calls
    this.username = username
}

function createUser(username, email, password){
    SetUsername(username)

    this.email= email
    this.password= password
}

const chai = new createUser("chai","chai@fb.com", "123")
console.log(chai);
// i have to read all the source carefully and revision of  javascript 
// so all the react and the system work propline
// so nice al the all the concept of all react work and the system of all the sources and the system
// to all the alert so this is known as the 